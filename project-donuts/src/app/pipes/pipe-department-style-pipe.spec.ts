import { PipeDepartmentStylePipe } from './pipe-department-style-pipe';

describe('PipeDepartmentStylePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeDepartmentStylePipe();
    expect(pipe).toBeTruthy();
  });
});
